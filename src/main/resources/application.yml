server:
  port: 8080
  strategy: WEIGHTED_ROUND_ROBIN
  # Available values:
  # - ROUND_ROBIN
  # - WEIGHTED_ROUND_ROBIN

health-check:
  enabled: true
  duration: 5
  path: /health

services:
  - id: backend-1
    host: localhost
    port: 9001
    weight: 1

  - id: backend-2
    host: localhost
    port: 9002
    weight: 2

  - id: backend-3
    host: localhost
    port: 9003
    weight: 1

  - id: backend-4
    host: localhost
    port: 9004
    weight: 1